# Python

Установка:

```bash
python -m pip install -r requirements.txt
```

Перед запуском измените имя порта в `demo.py`.

Библиотека намеренно не поддерживает диапазон 100–150 кГц, пока формат команды не будет подтвержден на реальном модуле.
